repositories {
    maven { url "https://artifactory.jointag.com/artifactory/jointag" }
}

dependencies {
   compile 'com.jointag:proximitysdk:1.10.1'
}

android {
  packagingOptions {
    exclude 'META-INF/NOTICE'
    exclude 'META-INF/LICENSE'
  }
}
